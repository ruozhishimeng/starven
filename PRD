# 📝 游戏产品需求文档 (PRD) - V0.1 生存原型

## 1. 核心概述

**游戏类型：** 2D俯视角，像素风，开放世界生存制作游戏

**核心循环：**
> 探索随机地图 → 收集基础资源（砍树/采摘） → 维持人物状态（饱食/生命） → 制作工具/烹饪食物 → 击杀怪物获取高级资源 → 升级

**架构准则：**
> 代码与数据分离。所有的物品、配方、怪物属性必须通过 Godot 的 `.tres` (Resource) 文件配置，避免硬编码，方便后续 AI 快速批量生成内容。

---

## 2. 系统需求与模块拆解

### 2.1 角色系统 (Player & Stats)

作为游戏的核心，主角需要一个易于扩展的状态机和属性组件。

**基础属性组件 (Stats Component)：**

| 属性 | 说明 |
|------|------|
| 生命值 (Health) | 归零则死亡 |
| 饥饿值 (Hunger) | 随时间下降，降为0时开始扣除生命值 |
| 体力/理智 (Stamina/Sanity) | （初版可选）用于限制高强度劳作或夜晚的惩罚 |

**成长系统：**
- 经验值 (EXP) 与 等级 (Level)。砍树、采摘、击杀怪物可获得经验
- 初版升级效果：简单提升生命上限或基础攻击力

**动作状态机 (State Machine)：** 包含状态：Idle (空闲), Move (移动), Action (采集/攻击), Dead (死亡)

---

### 2.2 地图生成系统 (World Generation)

为了保证"每次开局地图随机"，我们将使用 Godot 内置的噪声算法。

**技术实现：** 使用 FastNoiseLite 结合 TileMapLayer (Godot 4.x)

**生成规则：**
- **生物群落 (Biomes)：** 初版实现 2-3 种基础地貌（例如：草地、森林、荒地）
- **资源散布：** 根据不同地貌的阈值，随机实例化（Instantiate）资源节点，如：草地上多浆果丛，森林里多树木

---

### 2.3 交互与环境系统 (Environment & Interaction)

玩家对环境的改造是生存游戏的灵魂。

**资源节点 (Resource Nodes)：**

| 节点 | 交互方式 | 掉落物 |
|------|----------|--------|
| 树木 (Trees) | 需要"工具"才能高效交互 | 木材 |
| 浆果丛 (Berry Bushes) | 可直接徒手采摘 | 浆果 |

**交互逻辑：** 使用 Area2D 作为玩家的"交互检测圈"。进入圈内的高亮目标可按键交互

---

### 2.4 物品、背包与工具系统 (Inventory & Items)

这是最需要"结构化"的部分。

| 模块 | 描述 | 实现方式 |
|------|------|----------|
| 物品数据 | 包含物品ID、名称、图标、堆叠上限、类型（消耗品/材料/装备） | 编写基础的 ItemData 继承自 Resource |
| 背包逻辑 | N格容量的数组，支持添加、丢弃、交换物品 | 创建 InventoryManager 节点（可作为全局 Autoload） |
| 工具/武器 | 拥有不同的"效率值"或"攻击力" | 在角色节点下挂载 Equipment 槽位，装备特定物品时替换玩家的攻击判定 |

---

### 2.5 制作与烹饪系统 (Crafting & Cooking)

- **制作 (Crafting)：** 在 UI 面板中，消耗背包内指定数量的材料，生成新物品（如：3木材=1木斧）
- **烹饪 (Cooking)：** 简单的营火交互。投入基础食材，经过计时器（Timer），产出能大幅恢复饥饿值的高级食物（如：烤浆果）

---

### 2.6 战斗与怪物系统 (Combat & Enemies)

初版战斗保持简单，采用直接的碰撞或 Hitbox（受击框）/ Hurtbox（伤害框）机制。

**敌人行为 (Enemy AI)：**
- 简单的状态机：Wander (漫游) → 发现玩家进入警觉圈 → Chase (追逐) → 达到攻击距离 → Attack (攻击)
- **掉落物 (Loot)：** 怪物死亡后，在其坐标实例化掉落物场景

---

## 3. 开发步骤与里程碑 (Roadmap)

为了让你能够顺畅地利用 AI 辅助编程，强烈建议按照以下从底层到表现层，从独立系统到系统交互的顺序进行。

### 📍 Phase 1: 基础框架与角色游历 (Day 1-2)
- [ ] 搭建 Godot 项目目录结构
- [ ] 创建 Player 场景（使用 CharacterBody2D），实现八向移动逻辑
- [ ] 实现核心 UI 面板（血条、饥饿条），并使其随时间/动作变化

### 📍 Phase 2: 世界生成与环境 (Day 3-5)
- [ ] 编写基于 FastNoiseLite 的随机地图生成脚本
- [ ] 制作树木和浆果丛的静态场景，并在地图生成时将它们布置在地图上

### 📍 Phase 3: 核心交互与背包系统 (Day 6-8)
- [ ] 定义 ItemData 资源类
- [ ] 开发基于数组的背包逻辑层（暂不需要复杂的UI，先通过打印台 print() 确认物品能否被正确拾取和扣除）
- [ ] 实现角色的检测圈（Area2D），能够拾取掉落物
- [ ] 连接 UI 与背包数据，实现可视化的格子背包

### 📍 Phase 4: 制作、烹饪与装备 (Day 9-10)
- [ ] 实现制作配方判定系统
- [ ] 实现装备木斧/武器时的属性切换
- [ ] 制作营火实体与简单的定时烹饪逻辑

### 📍 Phase 5: 战斗与闭环 (Day 11-14)
- [ ] 创建第一个怪物场景（如：史莱姆或野猪），加入简单的追逐 AI 和伤害框检测
- [ ] 添加玩家武器的攻击动画与伤害判定
- [ ] 加入经验值掉落与玩家升级逻辑